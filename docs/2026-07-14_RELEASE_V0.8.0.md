# Vibe Tree v0.8.0 发布文案

目标版本：`v0.8.0`。

这是 `v0.7.4` 之后的功能版本，新增 Kimi Code 本地 Token 统计，并修复跨午夜首次启动时可能漏记上一日 Codex 用量的问题。

## GitHub Release Notes

```md
# Vibe Tree v0.8.0

## Added

- Added Kimi Code token tracking from local `~/.kimi-code/sessions/**/wire.jsonl` usage records.
- Added Kimi Code settings, source availability, source filters, model breakdowns, history charts, and custom session path support.
- Preserved multi-agent Kimi sessions by reporting sub-agent usage under distinct source labels.

## Fixed

- Fixed Codex usage inside the configured history window being skipped when its session file is under the previous calendar-day directory and Vibe Tree first starts after midnight.
- After upgrading, Vibe Tree rechecks Codex sessions inside the active statistics window to recover previously missed usage without counting existing data twice.

## Privacy

- Kimi Code session records are read locally. Prompts, replies, code files, local paths, and session text are not uploaded.
- No uploaded fields were added. When multi-device sync is enabled, Kimi Code uses the existing token, source, and model aggregate fields.

## Compatibility

- No Worker or D1 migration is required.
- Existing local usage data remains compatible. Codex sessions inside the active statistics window are rechecked once during upgrade.
```

## 发布前检查

- `package.json` 和 `package-lock.json` 版本号为 `0.8.0`。
- `updates/manifest.json` 首项为 `0.8.0`，发布链接指向 `v0.8.0`。
- `README.md`、`README.en.md` 和 `PRIVACY.md` 已补充 Kimi Code 数据来源与隐私说明。
- `npm run typecheck`
- `npm run build`
- `npm run test:codex-watcher`
- `npm run smoke:electron`
- `npm run verify:cloud-sync`
- `npm run verify:worker-api`
- `git diff --check`
